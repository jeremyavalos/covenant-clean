{
  "name": "covenant-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "mkdir -p dist && cp *.html styles.css dist/ && cp -R public/* dist/",
    "start": "python3 -m http.server 3000 --directory dist"
  }
}
